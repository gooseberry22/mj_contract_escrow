# Vite Environment Variables
# 
# Note: Only variables prefixed with VITE_ are exposed to the client-side code.
# For security, never expose sensitive data like API keys or secrets.

# Backend API URL
# Development: http://localhost:8000/api
# Production: Set this to your production API URL (e.g., https://api.yourdomain.com/api)
VITE_API_URL=http://localhost:8000/api

# Application Environment
# Options: development, production
# This is automatically set by Vite based on the mode, but can be overridden
# VITE_APP_ENV=development

# Feature Flags (optional)
# Uncomment and set to enable/disable features
# VITE_ENABLE_ANALYTICS=false
# VITE_ENABLE_DEBUG_MODE=false

# Public Configuration (optional)
# Any other public configuration that needs to be exposed to the client
# VITE_APP_NAME=TBA Surrogacy Escrow
# VITE_APP_VERSION=1.0.0
